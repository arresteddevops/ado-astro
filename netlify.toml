[build]
  command = "pnpm run build"
  publish = "dist/"

[build.environment]
  NODE_VERSION = "22"

[context.deploy-preview]
  command = "pnpm run build"

[context.branch-deploy]
  command = "pnpm run build"
